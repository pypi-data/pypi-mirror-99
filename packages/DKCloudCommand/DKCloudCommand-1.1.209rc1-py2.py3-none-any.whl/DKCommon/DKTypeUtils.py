from typing import Dict, Any, TypeVar

# Generic Type for use in various types
T = TypeVar("T")

# JSONData is a good alias to make it clear the dict structure for API calls
JSONData = Dict[str, Any]
