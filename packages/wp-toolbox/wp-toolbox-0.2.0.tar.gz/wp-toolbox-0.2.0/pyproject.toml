[tool.poetry]
name = "wp-toolbox"
version = "0.2.0"
description = ""
authors = ["Thomas Zastrow <thomas.zastrow@mpcdf.mpg.de>"]

[tool.poetry.dependencies]
python = "^3.7"

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
