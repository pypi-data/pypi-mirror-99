#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['yaqd_mqtt']

package_data = \
{'': ['*']}

install_requires = \
['yaqd-core>=2021.3.0', 'paho-mqtt']

extras_require = \
{'dev': ['black', 'pre-commit']}

entry_points = \
{'console_scripts': ['yaqd-mqtt-homie-sensor = '
                     'yaqd_mqtt._mqtt_homie_sensor:MQTTHomieSensor.main']}

setup(name='yaqd-mqtt',
      version='2021.3.1',
      description='yaq daemons for interacting with MQTT brokers',
      author='Blaise Thompson',
      author_email='blaise@untzag.com',
      url='https://yaq.fyi',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      extras_require=extras_require,
      entry_points=entry_points,
      python_requires='>=3.7',
     )
