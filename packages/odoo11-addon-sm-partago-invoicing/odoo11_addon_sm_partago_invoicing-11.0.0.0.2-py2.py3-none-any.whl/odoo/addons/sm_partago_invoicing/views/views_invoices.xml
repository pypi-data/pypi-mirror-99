<odoo>
  <data>
    <record id="view_invoices_form" model="ir.ui.view">
      <field name="name">sm_partago_invoicing.invoices.form</field>
      <field name="model">account.invoice</field>
      <field name="type">form</field>
      <field name="inherit_id" ref="account.invoice_form"/>
      <field name="arch" type="xml">
        <xpath expr="//field[@name='payment_term_id']" position="after">
          <field name="invoice_email_sent"/>
          <field name="invoice_report_id"/>
          <field name="invoice_template"/>
          <field name="prepaid_pocketbook"/>
        </xpath>
      </field>
    </record>
    <!-- TODO: This is going to be used if we enable account payment for clients -->
    <!-- <record id="view_invoices_search" model="ir.ui.view">
      <field name="name">sm_invoices_reports_batch.invoices.search</field>
      <field name="model">account.invoice</field>
      <field name="inherit_id" ref="account.view_account_invoice_filter"/>
      <field name="arch" type="xml">
        <xpath expr="//search" position="inside">
          <filter name="has_outstanding" string="Has Outstanding" domain="[('has_outstanding','=',True)]"/>
        </xpath>
      </field>
    </record> -->
    <record id="view_send_invoice_email_workflow" model="ir.actions.server">
      <field name="name">Send invoice</field>
      <field name="type">ir.actions.server</field>
      <field name="model_id" ref="model_account_invoice"/>
      <field name="binding_model_id" ref="model_account_invoice"/>
      <field name="state">code</field>
      <field name="code">model.send_invoice_email_from_action()</field>
    </record>
  </data>
</odoo>
