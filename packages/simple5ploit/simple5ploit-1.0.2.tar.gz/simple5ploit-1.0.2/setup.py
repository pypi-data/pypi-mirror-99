#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['simple5ploit', 'simple5ploit.modules']

package_data = \
{'': ['*'],
 'simple5ploit': ['utils/*'],
 'simple5ploit.modules': ['exploits/*',
                          'exploits/internal/*',
                          'gather/*',
                          'gather/internal/*']}

install_requires = \
['prompt_toolkit', 'tabulate', 'python3-nmap']

entry_points = \
{'console_scripts': ['simple5ploit = simple5ploit.__main__:main']}

setup(name='simple5ploit',
      version='1.0.2',
      description='simple5ploit',
      author='Alexis Rodriguez',
      author_email='rodriguez10011999@gmail.com',
      url='https://github.com/binexisHATT/simple5ploit',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      entry_points=entry_points,
     )
