from setuptools import setup

setup(name='distribution-classes',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['distribution-classes'],
      zip_safe=False)
