<odoo>
  <data noupdate="1">

    <delete model="mail.template" id="mail.mail_template_data_notification_email_default"/>

    <record id="mail.mail_template_data_notification_email_default" model="mail.template">
      <field name="name">email_tmpl_notification</field>
      <field name="subject">
        ${object.subject or (object.record_name and 'Re: %s' % object.record_name) or (object.parent_id and object.parent_id.subject and 'Re: %s' % object.parent_id.subject) or (object.parent_id and object.parent_id.record_name and 'Re: %s' % object.parent_id.record_name)}
      </field>
      <field name="model_id" ref="mail.model_mail_message"/>
      <field name="auto_delete" eval="True"/>
      <field name="body_html"><![CDATA[
<div style="font-family: helvetica;font-size: 14px;">
${object.body | safe}
<br>
</div>
% if ctx.get('signature'):
  ${ctx['signature'] | safe}
% else:
  ${user.signature|safe}
% endif

      ]]></field>
    </record>

  </data>
</odoo>