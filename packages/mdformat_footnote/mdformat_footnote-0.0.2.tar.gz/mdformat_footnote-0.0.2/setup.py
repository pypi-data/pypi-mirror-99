#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['mdformat_footnote']

package_data = \
{'': ['*'],
 'mdformat_footnote': ['.pytest_cache/*',
                       '.pytest_cache/v/*',
                       '.pytest_cache/v/cache/*']}

install_requires = \
['mdformat >=0.5.3,<0.6.0', 'mdit-py-plugins']

extras_require = \
{'dev': ['pre-commit'], 'test': ['pytest~=6.0', 'coverage', 'pytest-cov']}

entry_points = \
{'mdformat.parser_extension': ['footnote = mdformat_footnote']}

setup(name='mdformat_footnote',
      version='0.0.2',
      description='An mdformat plugin for parsing/validating footnotes',
      author='Gaige B. Paulsen',
      author_email='gaige@cluetrust.com',
      url='https://github.com/gaige/mdformat-footnote',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      extras_require=extras_require,
      entry_points=entry_points,
      python_requires='>=3.6',
     )
