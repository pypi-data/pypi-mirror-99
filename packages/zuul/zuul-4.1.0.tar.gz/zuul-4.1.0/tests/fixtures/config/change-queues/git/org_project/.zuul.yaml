# This queue will be ignored since it is already defined in common-config
- queue:
    name: integrated
    per-branch: false

- project:
    gate:
      queue: integrated
      jobs:
        - project-test
