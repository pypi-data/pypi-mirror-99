---
- name: Ensure network is setup
  hosts: all
  become: true

  tasks:
    - name: configure hotplug interfaces
      shell: |
        set -ex
        ifup --allow=hotplug {{ network_secondary_interface }} || true
