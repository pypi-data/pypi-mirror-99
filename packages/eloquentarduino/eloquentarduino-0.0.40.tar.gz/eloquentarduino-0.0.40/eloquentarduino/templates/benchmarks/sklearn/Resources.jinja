#include "Classifier.h"


Eloquent::ML::Port::Classifier clf;
float X[{{ X | shape }}] = {{ X | to_array }};


void setup() {
    Serial.begin(115200);
    Serial.println("Begin...");
    // clf.begin();
}

void loop() {
    for (uint8_t i = 0; i < {{ X | length }}; i++) {
        Serial.println(X[i][0]);
        Serial.println(clf.predict(X[i]));
    }

    delay(1000);
}