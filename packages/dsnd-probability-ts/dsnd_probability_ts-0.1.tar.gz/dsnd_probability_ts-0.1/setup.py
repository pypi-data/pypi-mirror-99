from setuptools import setup

setup(name='dsnd_probability_ts',
      version='0.1',
      description='Gaussian and Binomial distributions',
      autho = "Till Saenger (original: Andrew Paster)",
      packages=['dsnd_probability_ts'],
      zip_safe=False)
