project: firefly-aws
provider: aws

contexts:
  firefly:
  firefly_aws:
    region: ${AWS_DEFAULT_REGION}
    bucket: firefly-aws
    is_extension: true
#    storage:
#      services:
#        s3: ~
#      default: s3

evnironments:
  dev: ~