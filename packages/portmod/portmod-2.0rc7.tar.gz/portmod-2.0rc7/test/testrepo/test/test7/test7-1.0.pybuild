import os
from pybuild import Pybuild1, InstallDir
from pybuild.info import P


class Package(Pybuild1):
    NAME = "Test"
    KEYWORDS = "test"
    DESC = "test desc"
    HOMEPAGE = "test homepage"
    IUSE = "foo bar baz"
    REQUIRED_USE = """
        foo? ( bar )
        ?? ( bar baz )
    """
    RDEPEND = """
        foo? ( test/test )
        bar? ( test/test3 )
        baz? ( test/test5 )
    """
    INSTALL_DIRS = [InstallDir(".")]

    def src_prepare(self):
        os.makedirs(P)
        with open(os.path.join(P, "foo.txt"), "w") as file:
            print("foo", file=file)
