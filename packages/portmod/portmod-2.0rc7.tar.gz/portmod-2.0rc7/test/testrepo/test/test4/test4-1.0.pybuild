from pybuild import Pybuild1


class Package(Pybuild1):
    NAME = "Test"
    KEYWORDS = "test"
    LICENSE = "test"
    DESC = "test desc"
    HOMEPAGE = "test homepage"
    IUSE = "foo bar baz baf"
    REQUIRED_USE = """
        foo? ( bar )
        ?? ( bar baz baf )
    """
