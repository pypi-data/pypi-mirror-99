from setuptools import setup

setup(name='col-dist',
      version='0.1',
      description='Gaussian distributions',
      packages=['col-dist'],
      author = "Francisco Colina",
      zip_safe=False)
