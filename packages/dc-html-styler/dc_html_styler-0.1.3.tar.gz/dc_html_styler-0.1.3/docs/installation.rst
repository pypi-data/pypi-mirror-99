.. highlight:: shell

============
Installation
============


Stable release
--------------

To install dc_html_styler, run this command in your terminal:

.. code-block:: console

    $ pip install dc_html_styler

This is the preferred method to install dc_html_styler, as it will always install the most recent stable release.

If you don't have `pip`_ installed, this `Python installation guide`_ can guide
you through the process.

.. _pip: https://pip.pypa.io
.. _Python installation guide: http://docs.python-guide.org/en/latest/starting/installation/


From sources
------------

The sources for dc_html_styler can be downloaded from the `Github repo`_.

You can either clone the public repository:

.. code-block:: console

    $ git clone git://github.com/jejdc/dc_html_styler

Or download the `tarball`_:

.. code-block:: console

    $ curl -OJL https://github.com/jejdc/dc_html_styler/tarball/master

Once you have a copy of the source, you can install it with:

.. code-block:: console

    $ python setup.py install


.. _Github repo: https://github.com/jejdc/dc_html_styler
.. _tarball: https://github.com/jejdc/dc_html_styler/tarball/master
