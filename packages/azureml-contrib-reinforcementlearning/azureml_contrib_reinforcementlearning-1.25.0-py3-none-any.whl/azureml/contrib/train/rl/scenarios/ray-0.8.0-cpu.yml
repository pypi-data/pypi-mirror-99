dockerFile: "FROM mcr.microsoft.com/azureml/base:intelmpi2018.3-ubuntu16.04\nRUN apt-get update && apt-get install -y --no-install-recommends ffmpeg && apt-get clean -y && rm -rf /var/lib/apt/lists/* && rm -rf /usr/share/man/*"
inlineCondaDependencies:
  dependencies:
  - python=3.6.2
  - pip:
    - azureml-defaults
    - azureml-contrib-reinforcementlearning
    - tensorflow==1.14.0
    - ray[rllib,dashboard]==0.8.0
    - psutil
    - setproctitle
    - gym[atari]
  