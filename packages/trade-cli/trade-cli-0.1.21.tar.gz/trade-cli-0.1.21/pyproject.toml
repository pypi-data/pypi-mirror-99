[tool.poetry]
name = "trade-cli"
version = "0.1.21"
description = ""
authors = ["Matyi <mmatyi@caesar.elte.hu>"]

[tool.poetry.dependencies]
python = "^3.8"
tqdm = "^4.46"
requests = "^2.23"
click = "^7.1"
tabulate = "^0.8.7"
pick = "^0.6.7"
windows-curses = "^2.1"

[tool.poetry.dev-dependencies]
pytest = "^3.0"

[tool.poetry.scripts]
# command_name = module_for_handler : function_for_handler
trade-cli="trade_cli.cli:hello"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"


