[tool.poetry]
name = "lwh"
version = "0.0.7"
description = ""
authors = ["jeffthorne <jthorne@u.washington.edu>"]
readme = "README.md"

[tool.poetry.scripts]
lwh = "lwh.main:app"

[tool.poetry.dependencies]
python = "^3.9"
typer = {extras = ["all"], version = "^0.3.2"}
Jinja2 = "^2.11.3"

[tool.poetry.dev-dependencies]
pytest = "^4.6"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
