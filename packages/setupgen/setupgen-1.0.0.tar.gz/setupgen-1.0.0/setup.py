
from setuptools import setup

setup(
    name='setupgen',
    version='1.0.0',
    description='Generate setup.py files.',
    long_description='Install and then run python -m setupgen --help windows/linux',
    packages=["setupgen"]
    )

