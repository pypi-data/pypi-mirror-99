[tool.poetry]
name = "tg-tool"
version = "0.4.4"
description = ""
authors = ["sellercoder <veniamin4e@gmail.com>"]
readme = "README.md"

[tool.poetry.scripts]
tg-tool = "tg_tool.main:app"

[tool.poetry.dependencies]
python = "^3.7"
typer = "^0.3.2"

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
