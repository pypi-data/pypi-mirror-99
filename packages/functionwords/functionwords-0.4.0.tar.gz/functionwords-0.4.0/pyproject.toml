[tool.poetry]
name = "functionwords"
version = "0.4.0"
description = "Better curated Chinese and English function words."
authors = ["Haining Wang <hw56@indiana.edu>"]
readme = "README.md"
license = "MIT"
repository = "https://github.com/Wang-Haining/functionwords"

[tool.poetry.dependencies]
python = "^3.8"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.13"]
build-backend = "poetry.masonry.api"
