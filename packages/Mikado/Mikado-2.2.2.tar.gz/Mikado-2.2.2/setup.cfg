[aliases]
test = pytest

[tool:pytest]
norecursedirs = datrie* build sample_data
addopts = --strict-markers -m "not slow and not triage"
markers = 
	slow
	triage

[egg_info]
tag_build = 
tag_date = 0

