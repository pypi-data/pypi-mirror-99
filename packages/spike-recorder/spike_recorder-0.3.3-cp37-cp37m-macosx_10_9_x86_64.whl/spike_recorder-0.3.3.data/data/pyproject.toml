[build-system]
requires = [
    "wheel", 
    "setuptools>=42", 
    "setuptools_scm[toml]>=3.4",
    "pybind11>=2.6.1",
    "cmake",
    "scikit-build",
    "ninja"
]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
write_to = "src/spike_recorder/version.py"