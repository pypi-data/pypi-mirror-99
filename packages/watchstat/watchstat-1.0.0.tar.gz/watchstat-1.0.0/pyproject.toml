# Project settings for the 'black' code formatter
[build-system]
requires = ['setuptools>=42', 'wheel']
build-backend = "setuptools.build_meta"

[tool.black]
line-length = 80
target-version = ['py34']
include = '\.py$'
exclude = '''

(
  /(
      \.git
  )/
)
'''
