#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['attribution', 'attribution.tests']

package_data = \
{'': ['*']}

install_requires = \
['attrs', 'click', 'jinja2', 'packaging', 'tomlkit']

entry_points = \
{'console_scripts': ['attribution = attribution.main:main']}

setup(name='attribution',
      version='1.5.2',
      description='Generate changelogs based on tag messages and shortlogs',
      author='John Reese',
      author_email='john@noswap.com',
      url='https://attribution.omnilib.dev',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      entry_points=entry_points,
      python_requires='>=3.6',
     )
