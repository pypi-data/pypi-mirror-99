from setuptools import setup

setup(name='dndy_distributions',
      version='0.1',
      description='Gaussian distributions',
      packages=['dndy_distributions'],
      author='Clera Gan',
      zip_safe=False)
