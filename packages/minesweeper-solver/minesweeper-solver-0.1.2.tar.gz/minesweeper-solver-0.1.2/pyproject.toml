[tool.poetry]
name = "minesweeper-solver"
version = "0.1.2"
description = ""
authors = ["Thomas Lee"]
license = "MIT"
packages = [
    { include = "minesweeper_solver" }
]
readme = "README.md"

[tool.poetry.scripts]
minesweeper_solver = "minesweeper_solver:main"

[tool.poetry.dependencies]
python = "^3.9"
numpy = "^1.20.1"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
