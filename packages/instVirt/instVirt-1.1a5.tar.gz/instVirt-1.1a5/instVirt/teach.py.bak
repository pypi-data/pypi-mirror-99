#!/usr/bin/env python3
from tkinter import Tk, Label, Button, Frame, StringVar
import os
class vteach(Tk):
    def __init__(self):
        os.system('echo "asl-lab (Academia do Software Livre)." &')
        super().__init__()
        super().geometry("370x420")
        super().title("Instrutor virtual")
        self.filename = ""
        self.sbtn = Button(self, text=" O software livre", command=self.slivre)
        self.bbtn = Button(self, text=" Boas vindas", command=self.bvindo)
        self.lbl1 = Label(self, text="Operando no modo texto")
        self.texto = StringVar()
        self.bbcp = Button(self, text=" Copiando arquivos/diretórios", command=self.cp)
        self.bbcd = Button(self, text=" Navegando pelo sistema", command=self.cd)
        self.bbfe = Button(self, text="Visualizando os tipos de dados", command=self.file)
        self.bbto = Button(self, text=" Criando, modificando informações de um arquivo", command=self.touch)
        self.sbtn.grid(row=10, column=0)
        self.bbtn.grid(row=11, column=0)
        self.lbl1.grid(row=12, column=0)
        self.bbcd.grid(row=13, column=0)
        self.bbto.grid(row=14, column=0)
        self.bbfe.grid(row=15, column=0)
        self.bbcp.grid(row=16, column=0)
        pass
        
    def file(self):
        self.filename="/home/asl-teach/ASL-SL/FILE.txt"
        os.system("clear")
        talkStr = "cat " + self.filename + " | tsurya "
        texto = ""
        f = open(self.filename, "r")
        texto += f.read()
        print(texto)
        os.system(talkStr)
        pass

    def touch(self):
        self.filename="/home/asl-teach/ASL-SL/TOUCH.txt"
        os.system("clear")
        talkStr = "cat " + self.filename + " | tsurya "
        texto = ""
        f = open(self.filename, "r")
        texto += f.read()
        print(texto)
        os.system(talkStr)
        pass

    def cp(self):
        self.filename="/home/asl-teach/ASL-SL/CP.txt"
        os.system("clear")
        talkStr = "cat " + self.filename + " | tsurya "
        texto = ""
        f = open(self.filename, "r")
        texto += f.read()
        print(texto)
        os.system(talkStr)
        pass

    def cd(self):
        self.filename="/home/asl-teach/ASL-SL/CD.txt"
        os.system("clear")
        talkStr = "cat " + self.filename + " | tsurya "
        texto = ""
        f = open(self.filename, "r")
        texto += f.read()
        print(texto)
        os.system(talkStr)
        pass

    def slivre(self):
        self.filename="/home/asl-teach/ASL-SL/software_livre.txt"
        os.system("clear")
        talkStr = "cat " + self.filename + " | tsurya "
        texto = ""
        f = open(self.filename, "r")
        texto += f.read()
        print(texto)
        os.system(talkStr)
        pass
    
    def bvindo(self):
        self.filename="/home/asl-teach/ASL-SL/boas-vindas.txt"
        os.system("clear")
        talkStr = "cat " + self.filename + " | tbastet "
        texto = ""
        f = open(self.filename, "r")
        texto += f.read()
        print(texto)
        os.system(talkStr)
        pass

'''if __name__ == "__main__":
    app = vteach()
    app.mainloop()
'''
