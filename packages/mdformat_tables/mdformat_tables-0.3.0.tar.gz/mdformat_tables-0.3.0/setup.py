#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['mdformat_tables']

package_data = \
{'': ['*']}

install_requires = \
['mdformat>=0.6.0,<0.7.0']

extras_require = \
{'test': ['pytest~=6.0', 'coverage', 'pytest-cov']}

entry_points = \
{'mdformat.parser_extension': ['tables = mdformat_tables']}

setup(name='mdformat_tables',
      version='0.3.0',
      description='An mdformat plugin for rendering tables.',
      author='Chris Sewell',
      author_email='executablebooks@gmail.com',
      url='https://github.com/executablebooks/mdformat-tables',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      extras_require=extras_require,
      entry_points=entry_points,
      python_requires='>=3.6',
     )
