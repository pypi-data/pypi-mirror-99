[project]
requires-python = ">=3.7"

# Aside - in pyproject.toml you should always specify minimal build
# system options, like this:

[build-system]
requires = ["setuptools>=54", "wheel", "cffi"]
build-backend = "setuptools.build_meta"
