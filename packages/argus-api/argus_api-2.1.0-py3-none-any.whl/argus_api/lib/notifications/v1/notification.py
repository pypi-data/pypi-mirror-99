"""Autogenerated API"""
from argus_api import session



def prepare_notification(
    customerID: int = None,
    event: str = None,
    recipients: dict = None,
    attachments: dict = None,
    context: dict = None,
    json: bool = True,
    verify: bool = None,
    proxies: dict = None,
    apiKey: str = None,
    authentication: dict = {},
    server_url: str = None,
    body: dict = None,
  ) -> dict:
    """Prepare a new notification (DEV)
    
    :param int customerID: Set the customer context of this notification request. If not set, it should default to the current users customer. 
    :param str event: The name of the notification event to trigger. This will determine notification behaviour and rules. 
    :param list recipients: Recipients to notify, either user or non-user. 
    :param list attachments: Attachments to process in this notification. The notification rules and destinations will determine how these are used. 
    :param dict context: The notification context objects. The notification event determines which context objects are expected. Each object should be JSON serializable. 
    :param json:
    :param verify: path to a certificate bundle or boolean indicating whether SSL
    verification should be performed.
    :param apiKey: Argus API key.
    :param authentication: authentication override
    :param server_url: API base URL override
    :param body: body of the request. other parameters will override keys defined in the body.
    :raises AuthenticationFailedException: on 401
    :raises AccessDeniedException: on 403
    :raises ValidationErrorException: on 412
    :raises ArgusException: on other status codes
    
    :returns dictionary translated from JSON
    """

    route = "/notifications/v1/notification".format()

    headers = {
        'User-Agent': 'ArgusToolbelt/',
    }

    body = body or {}
    # Only send customerID if the argument was provided, dont send null values
    if customerID is not None:
        body.update({"customerID": customerID})
    # Only send event if the argument was provided, dont send null values
    if event is not None:
        body.update({"event": event})
    # Only send recipients if the argument was provided, dont send null values
    if recipients is not None:
        body.update({"recipients": recipients})
    # Only send attachments if the argument was provided, dont send null values
    if attachments is not None:
        body.update({"attachments": attachments})
    # Only send context if the argument was provided, dont send null values
    if context is not None:
        body.update({"context": context})

    query_parameters = {}

    

    response = session.post(
        route,
        params=query_parameters or None,
        json=body,
        verify=verify,
        apiKey=apiKey,
        authentication=authentication,
        server_url=server_url,
        headers=headers,
        proxies=proxies,
    )
    return response.json()



def prepare_notification_1(
    notificationID: str,
    json: bool = True,
    verify: bool = None,
    proxies: dict = None,
    apiKey: str = None,
    authentication: dict = {},
    server_url: str = None,
    body: dict = None,
  ) -> dict:
    """Send a pending notification (DEV)
    
    :param str notificationID: Notification ID
    :param json:
    :param verify: path to a certificate bundle or boolean indicating whether SSL
    verification should be performed.
    :param apiKey: Argus API key.
    :param authentication: authentication override
    :param server_url: API base URL override
    :param body: body of the request. other parameters will override keys defined in the body.
    :raises AuthenticationFailedException: on 401
    :raises AccessDeniedException: on 403
    :raises ObjectNotFoundException: on 404
    :raises ValidationErrorException: on 412
    :raises ArgusException: on other status codes
    
    :returns dictionary translated from JSON
    """

    route = "/notifications/v1/notification/{notificationID}/send".format(notificationID=notificationID)

    headers = {
        'User-Agent': 'ArgusToolbelt/',
    }

    body = body or {}

    query_parameters = {}

    

    response = session.put(
        route,
        params=query_parameters or None,
        json=body,
        verify=verify,
        apiKey=apiKey,
        authentication=authentication,
        server_url=server_url,
        headers=headers,
        proxies=proxies,
    )
    return response.json()

