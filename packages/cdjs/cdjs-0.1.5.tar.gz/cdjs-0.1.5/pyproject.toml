[project]
name = "cdjs"
repository = "https://github.com/ofhellsfire/cdjs"

[build-system]
build-backend = "maturin"
requires = ["maturin==0.9.3"]

[tool.maturin]
manylinux = "off"
sdist-include = ["Cargo.lock", "json/**/*"]
strip = "on"

