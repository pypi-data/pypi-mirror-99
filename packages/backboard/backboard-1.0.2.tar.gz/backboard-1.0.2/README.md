# backboard

Install with:

```
pip install backboard
```

Run it with:

```
backboard
```

###### It might take a while to boot up

To change the default keyboard sound setup use:

```
backboard -k SETUP
```